<div class="bar">
    <a href="/" target="_parent"><div class="top-logo">WhoQuote</div></a>
	<div class="gh"><i class="fa fa-github"></i></div>
</div>

<div class="wrapper">
	<div class="container">
		<div class="row">
			<div class="image-container col-sm-6 col-xs-8 col-sm-offset-3 col-xs-offset-2">
				<div class="quote-container">
					<div class="quote">{{vm.question.quote.text}}</div>
					<a href="{{ vm.question.quote.sourceURL }}" target="_blank"><i id="link" class="fa fa-link"></i></a>
				</div>
			</div>
		</div>

		<div class="row">
			<div class="people-container">
				{{#each vm.question.multipleChoiceSpeakers}}
					<div id="{{twitterId}}" class="person">
						<img class="twitter-pic" src="{{imageURL}}">
						<div>{{name}}</div>
						<small>@{{twitterHandle}}</small>
					</div>
				{{/each}}
			</div>
		</div>
		<div class="row">
			<div id="action-btn" class="action">Give up</div>
		</div>
	</div>
</div>
